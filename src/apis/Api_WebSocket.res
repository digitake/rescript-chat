let connect = () => {
  ()
}

let listenToChatroom = (~chatroomId as _) => {
  (async () => {
    // let chatroom = await Chatroom.findById(chatroomId)
    // chatroom.on('message', (message) => {
    //   console.log(message)
    // })
    ()
  })();
}